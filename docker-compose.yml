services:
  mongo:
    image: mongo:7
    container_name: finance_thesis_mongo
    ports:
      - "27017:27017"
    volumes:
      - mongo_data:/data/db
    environment:
      MONGO_INITDB_DATABASE: finance_thesis

volumes:
  mongo_data:
